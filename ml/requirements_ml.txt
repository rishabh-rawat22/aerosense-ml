# FILE: ml/requirements_ml.txt
# Python dependencies for the Aerosense ML microservice
# Install with: pip install -r requirements_ml.txt

# ── Core ML ───────────────────────────────────────────────────────────────────
torch==2.2.2              # PyTorch — LSTM model
numpy==1.26.4
scikit-learn==1.4.2       # StandardScaler, train_test_split

# ── Data / IO ─────────────────────────────────────────────────────────────────
pandas==2.2.2
pymongo==4.7.2            # MongoDB driver (matches Mongoose's MongoDB)
motor==3.4.0              # Async MongoDB driver (for FastAPI endpoints)
python-dotenv==1.0.1      # .env loading

# ── HTTP ──────────────────────────────────────────────────────────────────────
httpx==0.27.0             # Async HTTP client for OpenAQ API
requests==2.31.0          # Sync HTTP for backfill scripts

# ── API ───────────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# ── Scheduling ────────────────────────────────────────────────────────────────
apscheduler==3.10.4

# ── Logging / Utilities ───────────────────────────────────────────────────────
loguru==0.7.2
tqdm==4.66.4
